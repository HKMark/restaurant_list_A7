<h1 class="mb-1 restaurant-show-title">編輯餐廳 - {{restaurantsData.name}}</h1>
<div class="container">
  <form action="/restaurants/{{restaurantsData._id}}/edit" method="POST">
    <div class="mb-3">
      <label for="name" class="form-label">餐廳名稱(中文)<span class="text-danger">*</span></label>
      <input type="text" class="form-control" id="name" placeholder="Alpha 科技餐廳" name="name"
        value="{{restaurantsData.name}}" required />
    </div>
    <div class="mb-3">
      <label for="name_en" class="form-label">餐廳名稱(英文)<span class="text-danger">*</span></label>
      <input type="text" class="form-control" id="name_en" placeholder="Alpha Tech Theme Restaurant" name="name_en"
        value="{{restaurantsData.name_en}}" required />
    </div>
    <div class="mb-3">
      <label for="category" class="form-label">餐廳類別<span class="text-danger">*</span></label>
      <input type="text" class="form-control" id="category" placeholder="美式" list="categoryOptions" name="category"
        value="{{restaurantsData.category}}" required />
      <datalist id="categoryOptions">
        <option>美式</option>
        <option>咖啡</option>
        <option>中東料理</option>
        <option>日本料理</option>
        <option>義式餐廳</option>
      </datalist>
    </div>
    <div class="mb-3">
      <label for="image" class="form-label">餐廳圖片連結<span class="text-danger">*</span></label>
      <input type="url" class="form-control" id="image" name="image" value="{{restaurantsData.image}}" required />
    </div>
    <div class="mb-3">
      <label for="location" class="form-label">地址<span class="text-danger">*</span></label>
      <input type="text" class="form-control" id="location" placeholder="台中市北屯區迷有這條路520號" name="location"
        value="{{restaurantsData.location}}" required />
    </div>
    <div class="mb-3">
      <label for="phone" class="form-label">電話<span class="text-danger">*</span></label>
      <input type="tel" class="form-control" id="phone" placeholder="04-23456789" name="phone"
        value="{{restaurantsData.phone}}" required />
    </div>
    <div class="mb-3">
      <label for="google_map" class="form-label">Google地圖連結<span class="text-danger">*</span></label>
      <input type="url" class="form-control" id="google_map" name="google_map" value="{{restaurantsData.google_map}}"
        required />
    </div>
    <div class="mb-3">
      <label for="rating" class="form-label">餐廳評分<span class="text-danger">*</span></label>
      <input type="number" class="form-control" id="rating" min="0" max="5" step="0.1" name="rating"
        value="{{restaurantsData.rating}}" required />
    </div>
    <div class="mb-3">
      <label for="description" class="form-label">餐廳敘述<span class="text-danger">*</span></label>
      <textarea class="form-control" id="description" rows="3" name="description"
        required>{{restaurantsData.description}}</textarea>
    </div>
    <div class="text-center">
      <button type="submit" class="btn btn-primary">
        儲存修改
      </button>
      <button type="reset" class="btn btn-outline-danger">還原輸入</button>
    </div>
  </form>
</div>
